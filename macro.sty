\ProvidesPackage{macro}

\RequirePackage[T1]{fontenc}
\RequirePackage{textcomp}
\RequirePackage[utf8]{inputenc}
\RequirePackage[english,italian]{babel}
\RequirePackage{fourier}
\RequirePackage{microtype}
\RequirePackage[margin=4cm]{geometry}
\RequirePackage{array}
\RequirePackage[grid]{multicol}
\RequirePackage{indentfirst}
\RequirePackage{pifont}
\RequirePackage{guit}
\RequirePackage{graphicx}
\RequirePackage{xcolor}
\RequirePackage{tocloft}

\renewcommand{\cftsecfillnum}[1]{%
\mbox{\normalfont\ \textbullet\ }\nobreak
\mbox{\cftsecpagefont #1}\cftparfillskip\par}

\usepackage{fancyvrb}
\fvset{xleftmargin=\the\parindent}

\RequirePackage[babel]{csquotes}
\RequirePackage[citestyle=authoryear-comp,backref,hyperref]{biblatex}
\bibliography{biblio}

\providecommand{\emacs}{Emacs}
\providecommand{\auctex}{AUC\TeX{}}
\providecommand{\mano}{\ding{42}}

\providecommand{\titolo}{Una introduzione a GNU Emacs \& AUC\TeX{}}
\providecommand{\autorea}{Orlando Iovino}
\providecommand{\autoreb}{Elrond}   % mettere nome e cognome
\providecommand{\autorec}{matteo.l} % mettere nome e cognome
\providecommand{\autori}{\autorea, \autoreb, \autorec}

\setcounter{secnumdepth}{1}
\setcounter{tocdepth}{1}

\makeatletter
\newlength\dimfig
\newcommand*\mettifigura[1]{%
\par\noindent
\setbox\z@\hbox{#1}%
\dimfig=\wd\z@%
\hangindent\dimfig\hangafter-2\relax
\raisebox{-\baselineskip}[0pt][0pt]{\llap{\box\z@}}}
\makeatother

\RequirePackage{hyperref}
\hypersetup{%
  pdftitle={\titolo},
  pdfauthor={\autori},
  pdfsubject={Breve guida all'uso di GNU Emacs e AUCTeX per Windows,
    GNU Linux e Mac OS},
  pdfcreator={LaTeX}
}

%%% Local Variables:
%%% mode: latex
%%% coding: utf-8
%%% TeX-master: "main"
%%% End:
